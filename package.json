{
  "name": "propline-clv-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "CLV tracker for sports bets — graded against PropLine closing-line history",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "propline-clv-tracker": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --import tsx/esm src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "propline": "^0.2.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
